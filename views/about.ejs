<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs') %>
</head>
<body>
    <% console.log(students); %>
    <h1><%= heading %></h1>
    <h2>About Our Students</h2>
    <ul>
        <% students.forEach(student => { %>
            <li>
                <%= student.name %> - 
                <%= student.age %> years old - 
                <%= helloMessage(student.name) %>
            </li>
        <% }); %>
    </ul> 
</body>
</html>